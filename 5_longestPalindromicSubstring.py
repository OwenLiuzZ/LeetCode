class Solution(object):
    def longestPalindrome(self, s):
        """
        :type s: str
        :rtype: str
        """
        longestPalindrome = ''
        if len(s) % 2 == 1:
            mid = head = tail = len(s) / 2
            for i  in range(len(s)):



        else:


